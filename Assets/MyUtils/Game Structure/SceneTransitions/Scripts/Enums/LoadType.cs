namespace My_Utils
{
    /// <summary>
    /// Transition -> Transition between different scenes
    /// Reload -> Load the same scene
    /// </summary>
    public enum LoadType { Transition, Reload };
}
