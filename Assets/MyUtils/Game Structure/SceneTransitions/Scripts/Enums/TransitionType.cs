namespace My_Utils
{
    /// <summary>
    /// Animated -> A transition using transition animations
    /// NotAnimated -> A transition without any transition animations
    /// </summary>
    public enum TransitionType { Animated, NotAnimated };
}
