using UnityEngine;
using TMPro;

namespace My_Utils
{
    /// <summary>
    /// Atualize the value of the resource text based on saved data.
    /// </summary>
    public class ResourceField : MonoBehaviour
    {
        [SerializeField]
        private TextMeshProUGUI amountText;

        private void Awake()
        {

        }

        // Atualize field based on saved data
    }
}
